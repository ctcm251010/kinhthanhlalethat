{
  "name": "kinh-thanh-la-le-that",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "typecheck": "astro sync && tsc --noEmit",
    "check:links": "node scripts/check-links.mjs",
    "cms": "decap-server",
    "deploy": "npm run build && wrangler deploy",
    "oauth:types": "wrangler types --config workers/decap-oauth/wrangler.jsonc workers/decap-oauth/worker-configuration.d.ts",
    "oauth:typecheck": "tsc --noEmit --project workers/decap-oauth/tsconfig.json",
    "oauth:test": "node --test workers/decap-oauth/test/*.test.mjs",
    "oauth:check": "npm run oauth:types && npm run oauth:typecheck && npm run oauth:test",
    "oauth:deploy": "wrangler deploy --config workers/decap-oauth/wrangler.jsonc"
  },
  "dependencies": {
    "@astrojs/mdx": "8.0.1",
    "@astrojs/sitemap": "3.7.4",
    "@fontsource/noto-sans": "5.3.0",
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "decap-server": "3.11.2",
    "typescript": "6.0.3",
    "wrangler": "4.131.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
